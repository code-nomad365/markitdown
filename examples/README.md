# MarkItDown Excel 轉換範例

## 範例：學校預算表轉 Markdown

這個範例展示如何使用 MarkItDown CLI 將複雜的 Excel 預算表轉換為 Markdown 格式。

### 範例檔案

- **school_budget.xlsx** - 明德國小 2024 年度預算表
- **school_budget_output.md** - 轉換後的 Markdown 輸出

### Excel 檔案結構

這個範例包含三個工作表：

1. **年度預算總覽** - 按季度分類的預算摘要
   - 教學支出、行政支出、設施維護、活動經費、其他支出
   - 包含季度數據和年度總計

2. **教學支出明細** - 教學類別的詳細分項
   - 教科書費、教材教具、教師培訓等
   - 包含預算金額和備註說明

3. **預算執行率** - 預算執行狀況追蹤
   - 預算金額 vs 實際支出
   - 自動計算執行率百分比

### 使用 CLI 轉換

#### 基本轉換命令

```bash
# 轉換 Excel 檔案並輸出到螢幕
markitdown school_budget.xlsx

# 指定輸出檔案
markitdown school_budget.xlsx -o school_budget_output.md
```

#### 轉換結果

MarkItDown 會：
- ✅ 將每個工作表轉換為獨立的 Markdown 章節
- ✅ 保留表格結構為 Markdown 表格格式
- ✅ 維持數據的組織結構
- ✅ 支援多工作表處理

### 輸出範例

轉換後的 Markdown 格式：

```markdown
## 年度預算總覽

| 預算類別 | 第一季 | 第二季 | 第三季 | 第四季 | 年度總計 |
| --- | --- | --- | --- | --- | --- |
| 教學支出 | 450000 | 480000 | 460000 | 490000 | 1880000 |
| 行政支出 | 180000 | 190000 | 185000 | 195000 | 750000 |
| 設施維護 | 120000 | 150000 | 140000 | 130000 | 540000 |
...
```

### 使用場景

這個範例適用於：
- 📊 財務報表轉換
- 📝 預算文件整理
- 🤖 將 Excel 數據提供給 LLM 分析
- 📄 產生可讀性高的報告文件

### Python API 範例

你也可以使用 Python API 進行轉換：

```python
from markitdown import MarkItDown

# 初始化轉換器
md = MarkItDown()

# 轉換 Excel 檔案
result = md.convert("school_budget.xlsx")

# 輸出 Markdown 內容
print(result.text_content)

# 或儲存到檔案
with open("output.md", "w", encoding="utf-8") as f:
    f.write(result.text_content)
```

### 注意事項

1. **公式計算**：MarkItDown 讀取儲存在 Excel 中的計算值，如果檔案未計算過，可能顯示為空值
2. **格式化**：複雜的格式（顏色、邊框等）不會保留，專注於內容結構
3. **合併儲存格**：合併的儲存格會展開為個別欄位
4. **編碼**：建議使用 UTF-8 編碼以正確顯示中文

### 進階用法

```bash
# 只處理特定工作表（需透過 Python API）
# 使用管道處理
cat school_budget.xlsx | markitdown > output.md

# 批次轉換多個檔案
for file in *.xlsx; do
    markitdown "$file" -o "${file%.xlsx}.md"
done
```

### 相關資源

- [MarkItDown GitHub](https://github.com/microsoft/markitdown)
- [完整文檔](https://github.com/microsoft/markitdown/blob/main/README.md)
- [Python API 使用指南](https://github.com/microsoft/markitdown#python-api)
